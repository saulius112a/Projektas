<h2>Upload File</h2>
@using (Html.BeginForm("UploadAttributes", "Category", null, FormMethod.Post, new { enctype = "multipart/form-data" }))
{
    @Html.AntiForgeryToken()
    @Html.ValidationSummary()

    <div class="form-group">
        <input type="file" id="dataFile" name="failas" />
    </div>

    <div class="form-group">
        <input type="submit" value="Įkelti" class="btn btn-default" />
    </div>
}
